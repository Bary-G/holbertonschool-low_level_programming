#include "main.h"
#include <stdio.h>

/**
 * dog - structure
 * @name: the name
 * @age: the age
 * @owner: the owner
 *
 * Return: no
 */
typedef struct dog
{
	char *name;
	float age;
	char *owner;
}
dog_t;
