#include "main.h"
#include <stdio.h>

/**
 * _isupper - my function
 * @a: my value
 * @b: my variable
 * @product: my product
 *
 * Return: my function
 */

int mul(int a, int b)
{
	int product = multiply(num1, num2);

	printf("%d\n", num1, num2, product);
	return (0);
}
