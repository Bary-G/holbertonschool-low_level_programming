#include "main.h"
#include <stdio.h>

/**
 * _puts_recursion - my function
 * @s: my pointer
 *
 * Return: my function
 */

void _puts_recursion(char *s)
{
	if (*s == '\0')
	{
		_putchar('\n');
		return (NULL);
	}
	_putchar(*s);
	_puts_recursion(s + 1);
}
