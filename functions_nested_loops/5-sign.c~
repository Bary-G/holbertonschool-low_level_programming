#include "main.h"
#include <stdio.h>

/**
 * print_sign - my function
 * @n: my value
 *
 * Return: my function
 */

int print_sign(int n)
{
	if (n > 0)
	{
		return (1);
	}
	else if (n == 0)
	{
		return (0);
	}
	else
	{
		return (-1);
	}

}
