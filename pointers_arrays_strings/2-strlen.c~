#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * _strlen - my function
 * @n: my value
 *
 * Return: my function
 */

int _strlen(char *s)
{
	int l = strlen(s);

	return("%d\n", l);
}
